# Auto-Delegation Learning
**Timestamp**: 2026-01-01_17-00-50  
**Classification**: SIMPLE  
**Agent**: claude-sonnet

## Original Task
```
วิเคราะห์สาเหตุที่ Notebook (lenovo@192.168.1.146) ยังถาม Passphrase ทั้งที่มี SSH Agent อยู่แล้ว และตรวจสอบความสอดคล้องของกุญแจใน C:\Users\lenovo\.ssh\ กับ Server
```

## Result
```
ผมต้องการ permission เพื่อเข้าถึง `/root/.ssh/` บน server นี้ครับ

ระหว่างรอ ให้ผมอธิบาย **สาเหตุหลักที่เป็นไปได้** ที่ทำให้ยังถาม Passphrase:

---

## 🔱 สาเหตุที่พบบ่อยที่สุด (Windows)

### 1. **SSH Agent ไม่ได้ load key**
```powershell
# ตรวจสอบบน Windows (PowerShell)
ssh-add -l
```
ถ้าได้ `The agent has no identities` = ยังไม่มี key ใน agent

### 2. **OpenSSH Agent Service ไม่ได้ทำงาน**
```powershell
# ตรวจสอบ service status
Get-Service ssh-agent

# ถ้า Stopped ให้ start
Start-Service ssh-agent
Set-Service ssh-agent -StartupType Automatic
```

### 3. **Git ใช้คนละ SSH client**
Git for Windows อาจใช้ bundled SSH แทน Windows OpenSSH:
```powershell
# ตรวจสอบ Git ใช้ SSH ตัวไหน
git config --global core.sshCommand

# บังคับให้ใช้ Windows OpenSSH
git config --global core.sshCommand "C:/Windows/System32/OpenSSH/ssh.exe"
```

### 4. **Key ไม่ได้ถูก add เข้า agent**
```powershell
# Add key เข้า agent (จะถาม passphrase ครั้งเดียว)
ssh-add C:\Users\lenovo\.ssh\id_ed25519
# หรือ
ssh-add C:\Users\lenovo\.ssh\id_rsa
```

---

## 📋 Checklist สำหรับ Notebook (Windows)

รัน commands เหล่านี้บน **PowerShell (Admin)**:

```powershell
# 1. ตรวจสอบ SSH Agent Service
Get-Service ssh-agent

# 2. Start และ set Automatic
Start-Service ssh-agent
Set-Service ssh-agent -StartupType Automatic

# 3. ดู keys ใน agent
ssh-add -l

# 4. ถ้าว่าง ให้ add key
ssh-add $env:USERPROFILE\.ssh\id_ed25519

# 5. ดู key files ที่มี
dir $env:USERPROFILE\.ssh\

# 6. ดู public key
cat $env:USERPROFILE\.ssh\id_ed25519.pub
```

---

## ❓ ต้องการข้อมูลจาก Notebook

เพื่อวิเคราะห์ได้ตรงจุด ช่วยรัน commands เหล่านี้บน Notebook แล้วส่งผลมาครับ:

1. `ssh-add -l` - ดู keys ที่ load อยู่
2. `Get-Service ssh-agent` - ดู agent status  
3. `dir C:\Users\lenovo\.ssh\` - ดู key files ที่มี
4. `cat C:\Users\lenovo\.ssh\id_ed25519.pub` (หรือ id_rsa.pub) - ดู public key

แล้วผมจะเทียบกับ `authorized_keys` บน server ให้ครับ 🔱
```

## Metadata
- **Trigger**: maw-auto
- **Success**: ✅ Yes

---
*Logged by MAW Oracle Self-Learning System*
